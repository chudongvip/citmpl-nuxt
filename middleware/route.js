/**
 * 1. query 缺少平台带平台
 * 2. 有 hash 带 hash
 * 3. vuex 记录平台
 * 4. 查询文档是否存在，不存在重定向
 * 5. 切换平台找相对应的文档
 * @param {*} context
 */
export default function (context) {
  // const { store, route, redirect } = context
}
